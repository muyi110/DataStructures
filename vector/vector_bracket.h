/*******************************************************/
/*
 * 通过下标访问向量元素（重载[]运算符）
 */
/*******************************************************/
#pragma once

template<typename T> T& Vector<T>::operator[](Rank r) const{ // 常量成员函数
    return _elem[r]; // 0 <= r < _size
}
